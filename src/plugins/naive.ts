import { create, NButton, NLayout, NLayoutSider, NMenu, NTable, NIcon, NDataTable } from 'naive-ui'

export default create({
  components: [NButton, NLayout, NLayoutSider, NMenu, NTable, NIcon, NDataTable]
})
